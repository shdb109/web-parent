package com.ldp.web.manager.mapper;

import com.ldp.web.manager.po.SysMenuRole;

public interface SysMenuRoleMapper {
    int insert(SysMenuRole record);

    int insertSelective(SysMenuRole record);
}